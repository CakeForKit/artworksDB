run:
  timeout: 5m
  modules-download-mode: readonly

linters:
  enable:
    - revive
    - gocyclo
    - govet
    - staticcheck
    - gosimple
    - unused
    - typecheck
    - ineffassign
    - gofmt
    - misspell

linters-settings:
  gocyclo:
    min-complexity: 10
  revive:
    rules:
      - name: exported
        disabled: false
      - name: line-length-limit
        disabled: false
        arguments: [120]

issues:
  max-issues-per-linter: 0
  max-same-issues: 0