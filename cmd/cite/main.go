package main

func main() {
	// r := gin.Default()

	// artworkRep, err := artworkrep.NewArtworkRep(ctx, pgCreds, dbCnfg)
	// if err != nil {
	// 	panic(err)
	// }
	// eventRep, err := eventrep.NewEventRep(ctx, pgCreds, dbCnfg)
	// if err != nil {
	// 	panic(err)
	// }
	// searcherServ := searcher.NewSearcher(artworkRep, eventRep) // Замените на вашу реализацию

	// // Инициализация обработчиков
	// handler := NewWebHandler(searcherService)

	// // Настройка маршрутов
	// r.GET("/artworks", handler.GetArtworks)
	// r.GET("/events", handler.GetEvents)

	// // Статические файлы
	// r.Static("/static", "./static")

	// // Запуск сервера
	// r.Run(":8080")
}
